server:
  port: 8080
  mode: debug

database:
  driver: sqlite
  dsn: ./data/memory.db

ai_service:
  base_url: http://localhost:8001

storage:
  type: local
  local_path: ./data/storage
  base_path: /memory

log:
  level: debug
  output: stdout